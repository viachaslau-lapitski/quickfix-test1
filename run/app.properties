tps=400
prod=1
len=17000
store=file
log=none
# inflight: max concurrent in-flight writes throttled at application level.
# SSLWriteThrottleFilter.acquire() is called in the producer before sendToTarget();
# the filter releases in messageSent(). Sized to match SSLHandlerG1.MAX_QUEUED_MESSAGES.
inflight=64
